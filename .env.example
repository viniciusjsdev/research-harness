# Copy to .env only for local script configuration.
# Do not commit real tokens.

# Optional academic/search providers
OPENALEX_MAILTO=
SEMANTIC_SCHOLAR_API_KEY=
CROSSREF_MAILTO=

# Optional Hermes/OpenAI/Codex configuration lives outside this repo.
# The Hermes auth store should stay in HERMES_HOME or ~/.hermes.
